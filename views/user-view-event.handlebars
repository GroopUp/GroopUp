<div class="jumbotron">
<h1><center>GroopUp</center></h1>
</div>
<div class="container">
<div class="row view-event">
    <div class ="col-lg-6 event">
      <h2>{{this.title}}</h2>
      <h4>{{this.location}}</h4>
      <h4>{{this.date}}</h4>
      <h4>{{this.time}}</h4>
      <h4>max attendance: {{this.attendance_cap}}</h4>
      <form action="/event-sign-up/{{this.id}}" method="POST">
        <button type="submit" class="btn btn-primary">join this event</button>
      </form>
    </div>
      <div class ="col-lg-6 pic">
      <img height="300px"  src="{{picture}}"/>



     </div>
   </div>
 </div>
 <div class="container footer">
 </div>
